{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Kestrel:EndpointDefaults:Protocols": "Http2", // Change the default protocol to HTTP/2 to bypass the GCP Cloud Run request size limit, which is 30 MB for HTTP/1.
  "AllowedHosts": "*"
}
